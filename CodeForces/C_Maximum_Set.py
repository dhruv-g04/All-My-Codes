T = int(input())
for tc in range(T):
    l,r=(map(int, input().split(' ')))
    l1=[x for x in range(l,r+1)]
    l2=l1.copy()
    # print(lst)
    i=1
    lst=[]
    m=0
    # while 2*i<=r:

    #     for j in range(i,r+1):
    #         if j%i==0:

    #             # for k in range (1):
    #             #     if j%lst[k][-1]==0:
    #             #         lst[k]+=[j]
    #             #         if len(lst[k])>m:
    #             #             m=len(lst[k])
    #             #     else:
    #             lst.append([i,j])
    i=0
    while len(l1)!=0:
        for  j in range (i,)
        
        i+=1
    print(lst)